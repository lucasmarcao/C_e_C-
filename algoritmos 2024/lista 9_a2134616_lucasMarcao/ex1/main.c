/*
descrição:
*/
#include <stdio.h>
#include <string.h> // strcat()
#include <stdlib.h> // malloc()

int main()
{
    printf(" \n \n");

    // espaço estrategico.

    // Inicio do programa.
    int pint = 0;

    // printf("digite um numero : \n");
    // scanf("%d", &pint);

    // while (pint > -1)
    // {
    //     printf("a: %d \n", pint);
    //     pint = pint - 1;
    // }

    // for (int i = pint; i >= 0; i--)
    // {
    //     printf("a: %d \n", i);
    // }

    // char *vetor[] = {"cdd", "dfkjdfjhfhd", "cdbcdfhdfh", "chupanigga",
    //                  "dsfhuidgfbduigdfinsghifdhrghjfgjhogfhjfgpjhfjpgihjgfhjf"};
    // int len = sizeof(vetor) / sizeof(char *);
    // printf("vetor de tamanho %d", len);

    // char palavra[] = "1ã3456789  1";
    // int quantasPalavras = ((sizeof(palavra) / sizeof(char)) - 1);
    // printf("palavra de tamanho %d : %s", quantasPalavras, palavra);

    int penis = rand();
    printf("aleatorio: %d", penis);

    // espaço estrategico.

    printf("\n \n \a");
    return 0;
}