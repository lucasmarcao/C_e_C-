
using namespace std;

int flocos = 789;

float multiplicaSenhor(float flutuante)
{
    cout << "\nflutuante -> " << flutuante << endl;
    cout << "\nvezes 3 -> " << (flutuante * 3) << endl;
    return (flutuante * 3);
}